name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: |
          rm -rf node_modules api/node_modules website/node_modules sdk/node_modules package-lock.json
          npm install
          npm ls vite --workspace=website
          test ! -e node_modules/rolldown
          test ! -e website/node_modules/rolldown

      - name: Build website
        env:
          GITHUB_PAGES: true
        run: npm run build --workspace=website

      - name: Add SPA fallback
        run: cp website/dist/index.html website/dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: website/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4