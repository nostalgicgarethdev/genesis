{
  "name": "genesis",
  "version": "0.1.0",
  "private": true,
  "description": "Genesis — The agent launchpad where only AI launches AI",
  "scripts": {
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=website && npm run build --workspace=sdk",
    "build:website": "npm run build --workspace=website",
    "build:sdk": "npm run build --workspace=sdk"
  },
  "workspaces": [
    "website",
    "sdk"
  ]
}